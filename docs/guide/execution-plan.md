---
title: 执行计划与状态
description: "企业级底座与完整版教程的一体化执行计划，按阶段列出当前状态、验收目标和下一步重点。"
---

# 执行计划与状态

这页只做一件事：把 `EZ Admin Gin` 当前这轮“企业级底座 + 完整版教程”重构的执行状态公开出来，方便跟踪主线是否走偏。

::: tip 🎯 怎么读这页
如果你想判断项目当前推进到哪一步，先看下面的阶段状态；如果你想跟着教程学习，还是以 [从零搭建教程](/tutorial/) 为主线。
:::

## 当前阶段判断

- 当前目标不是继续给 `v1` 打补丁，而是直接收敛到企业级完整版主线
- 当前主线要求代码和教程同步推进，不单独维护一套“简化版教程”
- 当前文档主线已经全部收稳，`Phase 4` 模块补齐已完成，下一批可复用模块也已经开始沿当前骨架继续扩展，同时保留 `Phase 6` 的交付验证入口

## 阶段清单

### Phase 1：最终结构定稿

- `[已完成]` 锁定单体 + `bootstrap / platform / module` 的最终骨架方向
- `[已完成]` 新增 `cmd/server` 入口，并让运行入口收敛到 `bootstrap`
- `[已完成]` 补 `platform` 命名空间，为认证、权限、配置、数据库、日志、迁移、Redis、数据权限留出稳定落点
- `[已完成]` 认证模块入口进入最终结构，`login / me / menus / dashboard` 已收敛到 `module/auth`
- `[进行中]` 继续把旧模块按最终结构往 `module/*` 收口

验收目标：

- 仓库主入口已经是最终结构，不再继续扩大旧的扁平目录
- 后续新增能力可以明确知道该落在哪一层

### Phase 2：组织体系与数据权限

- `[已完成]` 补部门、岗位、用户岗位、角色自定义部门范围这些基础模型
- `[已完成]` 给用户和角色模型补 `department_id`、`data_scope`
- `[已完成]` 补 PostgreSQL / MySQL 的组织体系与数据权限迁移脚本
- `[已完成]` 补 `Actor` 请求上下文装载能力
- `[已完成]` `/auth/me` 返回组织与数据范围摘要
- `[已完成]` 用户列表、编辑、状态修改、角色绑定开始接入数据权限查询作用域
- `[已完成]` 部门模块接入组织树与部门资源自身的数据范围过滤
- `[已完成]` 第 5 章数据权限核心正文已按“角色配置 → Actor → Scopes → 资源接入”主线展开，并补 Actor / 多角色并集 / 资源级接法两页正文
- `[待开始]` 抽出更稳定的 `ScopeResolver` 和资源级 `datascope.go`
- `[进行中]` 为部门、岗位和真实业务模块接入同样的数据权限链路

验收目标：

- 接口权限和数据权限已经正式拆成两条链路
- 当前登录人的部门、角色、数据范围可以被稳定读取和复用
- 至少一个核心资源已经真正按数据权限过滤

### Phase 3：核心系统模块最终化

- `[已完成]` 用户模块首个最终版示范：`entity / dto / repository / service / handler / routes / datascope`
- `[已完成]` 用户岗位关系正式接入用户模块接口和用户管理页
- `[已完成]` 角色模块进入最终结构，并接入 `data_scope` 与自定义部门范围配置
- `[已完成]` 菜单模块进入最终结构，并补齐统一 `policy` 元信息落点
- `[已完成]` 系统配置模块进入最终结构，并把配置读取与 Redis 缓存同步收口到独立服务
- `[已完成]` 文件模块进入最终结构，并把上传校验、落盘和元数据写入收口到独立服务
- `[已完成]` 操作日志模块进入最终结构，并明确“中间件写入 + 模块查询”的审计边界
- `[已完成]` 登录日志模块进入最终结构，并明确“登录入口写入 + 模块查询”的审计边界
- `[已完成]` 公告模块进入最终结构，并把轻量内容管理能力纳入统一系统模块边界
- `[已完成]` 部门模块进入最终结构，并接入组织树与部门范围过滤
- `[已完成]` 岗位模块进入最终结构，组织闭环补齐到“部门 + 岗位 + 用户归属”

验收目标：

- 系统核心模块不再依赖“Handler 直连 DB”作为长期结构
- 新增模块时，目录和职责不需要临时商量

### Phase 4：企业常用模块补齐

- `[已完成]` 数据字典：`sys_dict_type + sys_dict_item`、`system/dict` 模块、菜单按钮种子、前端管理页和一条成功路径自动化测试已补齐
- `[已完成]` 账户中心：当前登录人自助资料与密码修改已落在 `module/auth`，前端静态页入口、顶部账户入口和一条改昵称/改密码自动化测试已补齐
- `[已完成]` 附件中心：`sys_attachment`、`system/attachment` 模块、菜单按钮种子、前端管理页和一条上传/编辑/状态切换自动化测试已补齐，底层上传继续复用 `system/file`
- `[已完成]` 一个非 `system` 分组的真实业务示例模块：`crm/customer` 已补齐后端模块、菜单按钮种子、前端管理页、数据权限过滤和两条自动化测试
- `[已完成]` 业务动作层扩展示例：`crm/followup` 已补齐后端模块、菜单按钮种子、客户选项接口、前端管理页、数据权限过滤和两条自动化测试，证明真实业务模块可以继续长出第二层资源

验收目标：

- 项目不只是一套权限和菜单骨架，而是具备常见后台首版交付能力
- `Phase 4` 已经有四个真实能力按“后端 + 前端 + 文档 + 测试”闭环落地，其中数据字典、附件中心和 CRM 客户模块额外带有迁移与初始化种子

### Phase 5：完整版教程重写

- `[已完成]` 教程主线重排为 9 章
- `[已完成]` 教程首页、大纲、章节入口已按最终版结构重写
- `[已完成]` 第 3、4 章章节边界已拉直：第 3 章专注登录态，第 4 章专注 RBAC、Casbin 与菜单权限，章节路径和正文主题已重新对齐
- `[已完成]` 第 5 章正文已按“组织模型 → 角色范围 → Actor → 资源模式 → 模块落地 → 请求走读 → 验收清单”收成完整闭环，总览页、资源模式页、接入规范页与请求走读页的职责边界已重新拉直
- `[已完成]` 第 6 章正文已重写为系统模块主线：章节入口、模块固定结构、后端接入流程、示例模块、权限菜单与迁移说明已对齐 `module/system` 与 `module/iam/*`
- `[已完成]` 第 7 章章节入口、教程大纲与前端正文主线已经成形
- `[已完成]` 第 7 章已补前端运行时结构、管理台工程起步、登录态流转、登录页实现、后台布局、动态菜单注册、系统页面模式与四组详细案例
- `[已完成]` 第 8 章已补自己的后端模块结构、后端接入流程、权限菜单接入、前端页面接入、`crm/customer` 示例模块和模块验收清单，章节路径与正文主题已重新对齐
- `[已完成]` 第 9 章已补环境变量、部署验证、Compose 结构、Nginx/HTTPS、部署变体、更新回滚、回滚分级、部署排障、长期运维和项目复用十组正文
- `[已完成]` 同步补参考手册第一批高频工作手册：数据权限、DDL、权限码、环境变量、错误码、目录约定、模块规范、初始化数据、动态菜单白名单、查询分页、按钮权限消费示例、上传公开路径与模块初始化模板已补齐
- `[已完成]` 教程首页与教程大纲的章节完成度口径已重新对齐，第 6 章恢复为正式小节链接列表，第 6-9 章不再继续显示为统一“重构中”
- `[已完成]` 快速启动、项目结构、企业级架构升级三张总入口页已同步到当前主线阶段，不再继续保留“v2 第一阶段刚开始”或旧部署章节入口的口径
- `[已完成]` 站点侧边栏、首页教程特性卡片与文档基线清单已同步到当前 canonical 主线，旧章节残留页开始明确标注历史位置
验收目标：

- 教程读者看到的是“最终企业级版本怎么从 0 到 1 做出来”
- 文档不再依赖读者脑内补全“后面应该怎么升级”

### Phase 6：验证与交付基线

- `[已完成]` 后端 `go test ./...` 构建校验接通
- `[已完成]` 前端 `pnpm build` 构建校验接通
- `[已完成]` 文档 `pnpm docs:build` 构建校验接通
- `[进行中]` 最小后端自动化验证基线已建立：`setup/init` 首次初始化与重复初始化拒绝、登录成功/失败、`/auth/me`、一个 RBAC 拒绝场景、一个用户部门范围过滤场景，以及数据字典、附件中心、CRM 客户模块、CRM 客户跟进模块的成功路径和部门范围过滤场景已经纳入 HTTP 集成测试
- `[已完成]` 组织树数据权限补入自动化验证：`dept_and_children` 与 `self` 在 `datascope.DepartmentQueryScope(...)` 上已经有独立测试覆盖
- `[已完成]` 初始化迁移资产补入自动化验证：`server/main.go` embed 的 MySQL / PostgreSQL 迁移文件同步性与最新 `000009` 版本存在性已经有测试保护
- `[已完成]` 真实数据库迁移烟雾验证入口已补齐：`server/main_realdb_test.go` 增加 `TestEmbeddedMigrationsApplyOnRealDatabases`，`scripts/verify-realdb-migrations.sh` 可以拉起 PostgreSQL / MySQL 容器，验证迁移真实执行、重复执行幂等、`schema_migrations` 版本和关键表/种子存在
- `[待执行]` 在允许访问 Docker 的环境里实际运行这套双库迁移烟雾验证，补上一次可追溯的 PostgreSQL / MySQL 实测记录

验收目标：

- 项目质量标准不只靠“本地能跑”，而是有稳定的最小验证基线

## 现在最值得关注的三件事

1. 继续补齐下一批可复用业务模块，沿 `crm/customer -> crm/followup` 这类业务模块家族继续扩，不再停留在单点示例阶段。
2. 继续扩展 `Phase 6` 的更多关键失败场景验证，不只停留在成功链路与迁移烟雾层。
3. 在需要时于允许访问 Docker 的环境里运行 `scripts/verify-realdb-migrations.sh`，补上 PostgreSQL / MySQL 双库迁移实测记录。

## 下一步

- 想看最终教程入口：从 [教程首页](/tutorial/) 开始
- 想先理解设计思路：看 [企业级架构升级](/guide/enterprise-architecture)
- 想快速查数据范围规则：看 [数据权限模型](/reference/data-scope-model)
