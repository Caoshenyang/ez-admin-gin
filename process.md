# Process

本文件是当前阶段唯一执行依据。

- 更新时间：`2026-05-08`
- 适用范围：`server / admin / deploy` 代码本体
- 明确排除：`docs/` 不参与当前阶段完成判断
- 当前目标：先把代码结构和交付稳定性收成 `9 分` 水位，再把测试放到最后集中补

## 状态标记

- `[x]` 已完成
- `[-]` 进行中
- `[ ]` 未开始
- `[!]` 阻塞 / 当前失败

## 当前判断

### 已确认事实

- [x] 前端 `pnpm -C admin type-check` 通过
- [x] 前端 `pnpm -C admin build-only` 通过
- [x] 后端 `go build ./...` 通过
- [x] 前端主要管理页已经形成“页面装配 + composable 状态 + utils 纯函数”的收口方向，`post / attachment` 也已纳入同一模式；`LoginPage / AccountCenterPage / HealthView / DashboardHome` 这类功能页也开始按相同思路下沉脚本逻辑
- [x] 后端主要模块已经形成“routes + services + application ports + infra repository”的收口方向，`auth` 的受保护分组也已开始复用通用壳子
- [x] `process.md` 现已改为只维护最新执行计划，不再保留旧阶段过程记录

### 当前评分

- 后端落地度：`8.8 / 10`
- 前端落地度：`7.9 / 10`
- 整体代码质量：`8.2 / 10`
- 当前可交付信心：`8.0 / 10`

### 主要失分项

- [-] 前端页面模式已进一步统一，页面层主干已不再直接堆放列表/表单副作用，但还没有完全模板化到“新增页面无需再现场设计”
- [-] 后端模块装配方向已统一，但实现风格还没有完全拉齐
- [-] 人工链路回归还没有固化成稳定的执行清单
- [ ] 自动化测试暂时后置，当前还没有作为 9 分基线的一部分

## 总策略

本轮执行顺序固定如下：

1. 先收前端页面模式
2. 再收后端模块骨架
3. 再补人工验收与交付基线
4. 最后再集中补测试

说明：

- 当前阶段不允许把“补测试”提前成主任务
- 当前阶段所有完成判断优先以代码结构、构建结果、人工验收脚本化程度为准
- 当前阶段每完成一个部分，直接更新本文件，不再维护第二份 checklist

## 执行阶段

### Phase A：前端页面模式完全统一

目标：

把 `admin` 从“主要页面已收口”推进到“管理页模式基本统一，新增页面接入不再临场发挥”。

状态：

- [x] `menu / department / user / role / post / config / notice / dict / file / attachment / loginlog / operationlog` 已经开始按统一模式收口
- [x] 页面层主干已完成一轮扫描，`iam/pages` 和 `system/pages` 已不再直接承载 `useModalForm`、`useListLoader` 或创建/更新请求编排
- [x] `attachment` 页面内部的上传/编辑模型、默认值、payload 和规则也已下沉到独立 `types / utils`
- [x] `AccountCenterPage` 的资料加载、资料更新、密码修改和表单规则已下沉到独立 `composable / utils / types`
- [x] `HealthView` 的依赖卡片、探针说明、状态文案和加载逻辑已下沉到独立 `composable / utils / types`
- [x] `DashboardHome` 的指标卡片、快捷入口、健康摘要、工作台文案和加载逻辑已下沉到独立 `composable / utils / types`
- [x] `LoginPage` 的默认表单、验证码占位、页脚文案、提交流程和错误提示已下沉到独立 `composable / utils / types`
- [-] 还需要继续统一列表页空状态、一屏布局、筛选区、表格区和详情区的交互模式
- [ ] 还没有形成一份可以直接复用的前端页面接入模板

本阶段要做的事：

- 把剩余管理页继续压成统一结构：
  - 页面：只负责装配
  - composable：只负责状态和动作
  - utils：只负责默认值、回填、payload、格式化
- 检查并清理仍残留在页面或胖 composable 中的：
  - 默认查询
  - 表单回填
  - create/update payload
  - 成功提示文案
  - 空值显示和状态展示
- 统一主要管理页的一屏布局和滚动边界
- 产出一份“新增后台页面最小模板”的代码基线

完成标准：

- 新增一个后台管理页时，不需要重新决定页面分层方式
- 主要管理页的筛选、表格、弹窗、详情交互模式一致
- 页面层不再持续膨胀为逻辑堆积点

### Phase B：后端模块骨架完全统一

目标：

把 `server` 从“方向已经对了”推进到“新增模块时基本不用再讨论结构”。

状态：

- [x] `iam / system` 顶层已共用受保护路由组装器
- [x] 多数核心模块已具备 `services.go` 模块入口
- [x] `auth` 受保护接口分组已开始复用通用中间件壳子，不再在路由文件中重复拼装认证链路
- [-] 仍需继续检查是否还有路由层直接拼接依赖、跨层直连或风格不一致的位置
- [ ] 还没有形成足够明确的“新增模块最小模板”验收口径

本阶段要做的事：

- 继续统一模块结构：
  - `api/`
  - `application/`
  - `infra/`
  - `routes.go`
  - `services.go`
- 保证路由层只做装配，不直接承担具体依赖拼装细节
- 保证 application 层尽量只依赖 ports，不直接依赖 infra 细节
- 继续收紧模块间调用，优先通过模块入口能力复用，而不是跨层直连
- 识别并清理当前仍带有过渡态风格的模块

完成标准：

- 新增后端模块时基本不需要重新决定结构
- 同类模块的装配方式和 service/repository 组织方式一致
- 平台层与业务模块层的边界更稳定

### Phase C：人工验收与交付基线固化

目标：

在不先补测试的前提下，先把“怎么确认这版代码真能交付”固定下来。

状态：

- [-] 构建和类型校验已经稳定
- [ ] 关键业务链路还没有沉淀成统一的人工验收清单
- [ ] 发布前 / 合并前的最小检查步骤还没有固化

本阶段要做的事：

- 固定最小人工验收链路：
  - 初始化
  - 登录
  - 动态菜单
  - 用户 / 角色 / 部门
  - 配置 / 字典 / 文件 / 附件
  - 登录日志 / 操作日志
- 把这些链路整理成可重复执行的验收顺序
- 补齐必要的环境依赖说明，但仍以代码仓库本体为主
- 形成“当前代码可交付”的人工判断标准

完成标准：

- 任何一次大改后，都能按同一套步骤回归
- 不再依赖临时记忆判断“这次改动有没有把主链路打坏”
- 当前仓库具备稳定的人工交付基线

### Phase D：最后集中补测试

目标：

在结构和人工验收都收稳后，再补最值钱的测试，而不是提前陷入测试泥潭。

状态：

- [ ] 未开始

本阶段要做的事：

- 只补高价值测试，不追求铺量
- 优先级顺序：
  - 高价值纯函数 / utils 测试
  - 核心 application service 测试
  - 关键 HTTP 集成测试
  - 最小前端 E2E 主链路
- 测试内容必须服务当前主干，不补已经明确废弃的旧结构

完成标准：

- 关键主链路有基本保护网
- 后续继续重构时，能更早发现真实回归问题
- 测试成为 9 分以上基线的加分项，而不是阻塞项

## 当前优先级

只按下面顺序执行：

1. [x] 重写 `process.md`，清空旧阶段口径
2. [-] 继续推进 `Phase A`
3. [-] 并行推进 `Phase B`
4. [ ] `Phase A / B` 收稳后推进 `Phase C`
5. [ ] 最后再推进 `Phase D`

## 完成定义

当且仅当下面条件同时满足时，当前阶段可以认为达到 `9 分` 目标附近：

- [ ] 前端主要管理页模式完全统一
- [ ] 后端主要模块骨架完全统一
- [ ] 人工验收链路已经固定
- [ ] 构建、类型检查、后端编译持续稳定
- [ ] 最后补上的高价值测试开始形成保护网

## 默认约束

- 默认工作路径：`D:\A\ez-admin-gin`
- 默认以代码和本文件为唯一基线
- 默认测试放在最后，不提前打乱当前执行顺序
- 默认不再回写旧过程记录，只维护最新状态
