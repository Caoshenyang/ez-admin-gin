# EZ Admin 环境变量配置
# 上传到服务器后作为 .env 使用，由 systemd 服务文件加载。

# =====================================================================
# !! 必须修改 — 不替换将导致服务无法启动 !!
# =====================================================================

# JWT 签名密钥，生产环境必须替换为随机字符串（至少 32 字符）。
# 生成方法：openssl rand -hex 32
# 若 env=prod 且值仍包含 "change-me"，服务将拒绝启动。
EZ_AUTH_JWT_SECRET=change-me-to-a-random-string-at-least-32-chars

# =====================================================================
# 建议修改 — 使用默认值将存在安全风险
# =====================================================================

# 数据库密码，生产环境应使用强密码。
EZ_DATABASE_PASSWORD=ez_admin_123456

# Redis 密码，生产环境建议设置。
EZ_REDIS_PASSWORD=

# =====================================================================
# 数据库
# =====================================================================

# 后端直接运行在宿主机上，Docker 容器的端口已绑定到 127.0.0.1，所以用 127.0.0.1。
EZ_DATABASE_HOST=127.0.0.1
EZ_DATABASE_PORT=5432
EZ_DATABASE_USER=ez_admin
EZ_DATABASE_NAME=ez_admin

# =====================================================================
# Redis
# =====================================================================

EZ_REDIS_HOST=127.0.0.1
EZ_REDIS_PORT=6379

# =====================================================================
# 应用
# =====================================================================

EZ_APP_ENV=prod
EZ_SERVER_ADDR=:8080
EZ_LOG_LEVEL=info
EZ_LOG_FORMAT=json

# =====================================================================
# CORS（生产环境必须配置）
# =====================================================================

# 允许的跨域来源，多个用逗号分隔。
# 示例：EZ_CORS_ALLOWED_ORIGINS=https://admin.example.com
EZ_CORS_ALLOWED_ORIGINS=

# =====================================================================
# 限流
# =====================================================================

# 登录接口每 IP 在窗口期内最大请求数。
EZ_RATE_LIMIT_LOGIN_MAX_REQUESTS=10
# 登录接口限流窗口时间（秒）。
EZ_RATE_LIMIT_LOGIN_WINDOW_SEC=60
