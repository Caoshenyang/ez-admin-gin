# Manual Test Checklist

这份清单用于替代复杂自动化测试体系。每次发布、复制到新 MVP 项目、或改动权限/部署/系统模块后，按下面步骤做人工冒烟验证。

## 本地启动

- [ ] 执行 `make docker-up`，PostgreSQL 和 Redis 正常运行。
- [ ] 执行 `make server-dev`，后端监听 `http://localhost:8080`。
- [ ] 执行 `make install && make admin-dev`，前端监听 `http://localhost:5173`。
- [ ] 打开前端页面，没有白屏、资源 404 或控制台明显错误。

## 初始化管理员

- [ ] 调用 `POST /api/v1/setup/init` 创建首个管理员。
- [ ] 初始化接口返回成功，并提示及时修改默认密码。
- [ ] 再次调用初始化接口会被拒绝，避免重复初始化。

## 登录 / 登出

- [ ] 使用管理员账号登录成功。
- [ ] 登录后能进入 Dashboard。
- [ ] 刷新页面后登录态仍有效。
- [ ] 点击登出后回到登录页。
- [ ] 登出后再访问后台页面会被重定向到登录页。

## 未登录访问保护接口

- [ ] 不带 token 请求后台受保护接口，返回未认证错误。
- [ ] 使用无效 token 请求受保护接口，返回未认证错误。
- [ ] 使用过期 token 时，前端能进入预期的重新登录流程。

## 动态菜单加载

- [ ] 登录后左侧菜单能正常显示。
- [ ] 菜单层级、图标、页面标题和路由跳转正常。
- [ ] 刷新动态菜单页面后不出现 404。
- [ ] 无 component 或未知 component 的菜单能进入预期占位/回退页面。

## 用户管理

- [ ] 用户列表能加载、分页、按关键字筛选。
- [ ] 左侧部门树能筛选用户，点击“全部部门”后恢复全量列表。
- [ ] 角色筛选使用后端分页结果，列表数量和总数一致。
- [ ] 新增用户成功。
- [ ] 编辑用户基础信息成功。
- [ ] 分配角色、部门、岗位后保存成功。
- [ ] 禁用用户后不能继续作为正常用户使用。

## 角色管理

- [ ] 角色列表能加载、筛选。
- [ ] 新增角色成功。
- [ ] 编辑角色基础信息和数据权限成功。
- [ ] 给角色分配菜单权限成功。
- [ ] 给角色分配按钮权限成功。
- [ ] 给角色分配接口权限成功。
- [ ] 数据权限页能展示当前数据范围和自定义部门。
- [ ] 关联用户页能展示该角色下的用户列表。
- [ ] 角色禁用后权限效果符合预期。

## 权限分配

- [ ] 管理员拥有完整菜单和按钮权限。
- [ ] 普通角色只能看到已分配菜单。
- [ ] 普通角色只能使用已分配按钮。
- [ ] 修改权限后，重新登录能看到最新权限结果。

## 无权限访问拦截

- [ ] 普通角色直接访问未授权页面会被拦截。
- [ ] 普通角色直接调用未授权接口会被拒绝。
- [ ] 页面按钮权限与接口权限不冲突：按钮隐藏只是体验优化，接口仍有后端拦截。

## 部门 / 岗位

- [ ] 部门树能加载。
- [ ] 新增、编辑、停用、删除部门符合预期。
- [ ] 岗位列表能加载。
- [ ] 新增、编辑、停用、删除岗位符合预期。
- [ ] 用户绑定部门和岗位后，详情和列表展示正确。

## 数据权限

- [ ] 全部数据权限能看到所有目标数据。
- [ ] 本部门数据权限只看到本部门数据。
- [ ] 本部门及下级数据权限能看到下级部门数据。
- [ ] 仅本人数据权限只看到本人数据。
- [ ] 自定义部门数据权限只看到勾选部门范围内的数据。

## 系统模块

- [ ] 字典类型和字典项能新增、编辑、停用。
- [ ] 系统配置能新增、编辑、停用，并能按 key 查询。
- [ ] 操作日志能查询、筛选、查看详情。
- [ ] 登录日志能查询、筛选、查看详情。
- [ ] 通知公告能创建、发布、下线。
- [ ] 登录后通知抽屉能加载未读消息，WebSocket 通知连接不会被生产环境任意来源访问。
- [ ] 文件上传成功后，文件记录正确写入。
- [ ] 上传文件可通过公开路径访问。

## 后端构建

- [ ] 执行 `cd server && go mod tidy` 后，`go.mod` 和 `go.sum` 无非预期变更。
- [ ] 执行 `cd server && go vet ./...` 通过。
- [ ] 执行 `cd server && go build ./...` 通过。

## 前端构建

- [ ] 执行 `cd admin && pnpm install --frozen-lockfile` 通过。
- [ ] 执行 `cd admin && pnpm type-check` 通过。
- [ ] 执行 `cd admin && pnpm lint` 通过。
- [ ] 执行 `cd admin && pnpm check:api-types` 通过，确认生成 API 类型与 Swagger 同步。
- [ ] 执行 `cd admin && pnpm build` 通过。

## Docker Compose 配置校验

- [ ] 执行 `docker compose -f deploy/compose.local.yml config --quiet` 通过。
- [ ] 执行 `EZ_AUTH_JWT_SECRET=placeholder docker compose -f deploy/compose.prod.yml config --quiet` 通过。
- [ ] 执行 `docker compose -f deploy/compose.server.yml config --quiet` 通过。

## MVP 项目复用前检查

- [ ] 修改项目名称、Logo、页面标题和域名配置。
- [ ] 替换生产环境 `EZ_AUTH_JWT_SECRET`。
- [ ] 修改数据库、Redis、管理员账号和默认密码策略。
- [ ] 删除或隐藏不需要的系统菜单。
- [ ] 根据业务项目补充自己的模块、菜单、按钮权限和数据权限规则。
- [ ] 按本清单完成一次端到端人工冒烟验证。
